<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.PoiMapper">

    <resultMap id="PoiResultMap" type="com.example.demo.pojo.Poi">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="alias" property="alias"/>
        <result column="category" property="category"/>
        <result column="longitude" property="longitude"/>
        <result column="latitude" property="latitude"/>
        <result column="address" property="address"/>
        <result column="open_hours" property="openHours"/>
        <result column="suggested_duration_minutes" property="suggestedDurationMinutes"/>
        <result column="ticket_price" property="ticketPrice"/>
        <result column="atmosphere_tags" property="atmosphereTags"/>
        <result column="description" property="description"/>
        <result column="cover_image" property="coverImage"/>
        <result column="is_active" property="isActive"/>
    </resultMap>

    <select id="findById" resultMap="PoiResultMap">
        SELECT * FROM t_poi WHERE id = #{id}
    </select>

    <select id="listAllActive" resultMap="PoiResultMap">
        SELECT * FROM t_poi WHERE is_active = 1 ORDER BY id ASC
    </select>

    <select id="searchByAtmosphere" resultMap="PoiResultMap">
        SELECT * FROM t_poi
        WHERE is_active = 1
        <if test="tag != null and tag != ''">
            AND (atmosphere_tags LIKE CONCAT('%', #{tag}, '%') OR category LIKE CONCAT('%', #{tag}, '%'))
        </if>
        ORDER BY id ASC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_poi (
            name, alias, category, longitude, latitude, address,
            open_hours, suggested_duration_minutes, ticket_price,
            atmosphere_tags, description, cover_image, is_active
        ) VALUES (
            #{name}, #{alias}, #{category}, #{longitude}, #{latitude}, #{address},
            #{openHours}, #{suggestedDurationMinutes}, #{ticketPrice},
            #{atmosphereTags}, #{description}, #{coverImage}, #{isActive}
        )
    </insert>

    <update id="update">
        UPDATE t_poi
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="alias != null">alias = #{alias},</if>
            <if test="category != null">category = #{category},</if>
            <if test="longitude != null">longitude = #{longitude},</if>
            <if test="latitude != null">latitude = #{latitude},</if>
            <if test="address != null">address = #{address},</if>
            <if test="openHours != null">open_hours = #{openHours},</if>
            <if test="suggestedDurationMinutes != null">suggested_duration_minutes = #{suggestedDurationMinutes},</if>
            <if test="ticketPrice != null">ticket_price = #{ticketPrice},</if>
            <if test="atmosphereTags != null">atmosphere_tags = #{atmosphereTags},</if>
            <if test="description != null">description = #{description},</if>
            <if test="coverImage != null">cover_image = #{coverImage},</if>
            <if test="isActive != null">is_active = #{isActive},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_poi WHERE id = #{id}
    </delete>

</mapper>
