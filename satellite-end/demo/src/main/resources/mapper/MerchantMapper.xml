<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.MerchantMapper">

    <resultMap id="MerchantResultMap" type="com.example.demo.pojo.Merchant">
        <id column="id" property="id"/>
        <result column="amap_poi_id" property="amapPoiId"/>
        <result column="name" property="name"/>
        <result column="category" property="category"/>
        <result column="longitude" property="longitude"/>
        <result column="latitude" property="latitude"/>
        <result column="address" property="address"/>
        <result column="business_hours" property="businessHours"/>
        <result column="avg_price_per_person" property="avgPricePerPerson"/>
        <result column="flavor_tags" property="flavorTags"/>
        <result column="is_custom_added" property="isCustomAdded"/>
        <result column="phone" property="phone"/>
        <result column="rating" property="rating"/>
        <result column="is_active" property="isActive"/>
    </resultMap>

    <select id="findById" resultMap="MerchantResultMap">
        SELECT * FROM t_merchant WHERE id = #{id}
    </select>

    <select id="listAllActive" resultMap="MerchantResultMap">
        SELECT * FROM t_merchant
        WHERE is_active = 1
        ORDER BY is_custom_added DESC, rating DESC, id ASC
    </select>

    <select id="listWithFilters" resultMap="MerchantResultMap">
        SELECT * FROM t_merchant
        WHERE is_active = 1
        <if test="keyword != null and keyword != ''">
            AND (name LIKE CONCAT('%', #{keyword}, '%') OR address LIKE CONCAT('%', #{keyword}, '%') OR flavor_tags LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        <if test="category != null and category != ''">
            AND category = #{category}
        </if>
        <if test="isCustomAdded != null">
            AND is_custom_added = #{isCustomAdded}
        </if>
        ORDER BY is_custom_added DESC, rating DESC, id DESC
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO t_merchant (
            amap_poi_id, name, category, longitude, latitude, address,
            business_hours, avg_price_per_person, flavor_tags,
            is_custom_added, phone, rating, is_active
        ) VALUES (
            #{amapPoiId}, #{name}, #{category}, #{longitude}, #{latitude}, #{address},
            #{businessHours}, #{avgPricePerPerson}, #{flavorTags},
            #{isCustomAdded}, #{phone}, #{rating}, #{isActive}
        )
    </insert>

    <update id="update">
        UPDATE t_merchant
        <set>
            <if test="amapPoiId != null">amap_poi_id = #{amapPoiId},</if>
            <if test="name != null">name = #{name},</if>
            <if test="category != null">category = #{category},</if>
            <if test="longitude != null">longitude = #{longitude},</if>
            <if test="latitude != null">latitude = #{latitude},</if>
            <if test="address != null">address = #{address},</if>
            <if test="businessHours != null">business_hours = #{businessHours},</if>
            <if test="avgPricePerPerson != null">avg_price_per_person = #{avgPricePerPerson},</if>
            <if test="flavorTags != null">flavor_tags = #{flavorTags},</if>
            <if test="isCustomAdded != null">is_custom_added = #{isCustomAdded},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="rating != null">rating = #{rating},</if>
            <if test="isActive != null">is_active = #{isActive},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM t_merchant WHERE id = #{id}
    </delete>

</mapper>
